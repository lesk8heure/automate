Fichier sources de tout ce qui est lié à l'interface graphique

Pour installer wxWidgets sur votre ordi

lancer en root :
apt-get install libwxgtk2.8-dev wx-common

Voilà. C'est tout (on va tourner sur la 2.8 plutôt que la 3.0.2. Mieux intégrée)